#include <iostream>
#include "vec3.h"

int main() {

    const int width = 400;
    const int height = 400;

    //std::cout << "P3 ";
    //std::cout << width << " " << height << " ";
    //std::cout << 255 << "\n";

    //for (int i = 0; i < height; i++) {
    //    for (int j = 0; j < width; j++) {
    //        std::cerr << "Scanlines remaining: " << width - j << std::flush;
    //        double r = (double)i / width;
    //        double g = (double)j / height;
    //        double b = 0.25;

    //        int imageR = static_cast<int>(255.999 * r);
    //        int imageG = static_cast<int>(255.999 * g);
    //        int imageB = static_cast<int>(255.999 * b);

    //        std::cout << imageR << " " << imageG << " " << imageB << "\n";
    //    }
    //}
    //std::cerr << "\nDone render" << std::endl;
    return 0;
}
